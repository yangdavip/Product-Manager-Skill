
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
文档编号：SRS-[项目代号]-001
版本：V1.0
状态：草稿 / 评审中 / 基线 / 废止
创建日期：YYYY-MM-DD
最后更新：YYYY-MM-DD
作者：__
审批人：__
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━


修订记录

| 版本 | 日期 | 修订内容 | 修订人 | 审批人 |
| ---- | ---- | -------- | ------ | ------ |
| V1.0 | | 初始创建 | | |


## 1. 引言

### 1.1 目的
本文档描述 [产品名称] 的软件需求，作为开发、测试和验收的依据。

### 1.2 范围
- 系统名称：
- 功能概述：
- 不包含：

### 1.3 需求背景

### 1.4 需求目的
- 
- 
- 

### 1.5 定义与缩略语
| 术语/缩写 | 定义 |
|-----------|------|
| | |

---

## 2. 产品概述

### 2.1 产品背景

### 2.2 核心功能清单
+ 
+ 
+ 

### 2.3 功能逻辑

#### 业务流程图
*Mermaid 语法*

#### 类图 / 状态图 / 时序图（按需）
*Mermaid 语法*

#### 操作 × 状态矩阵（仅涉及状态流转的模块用）

| 操作 \ 状态 | 状态A | 状态B | 状态C |
|-------------|-------|-------|-------|
| 操作1 | ✅ | ✅ | ❌ |
| 操作2 | ❌ | ✅ | ❌ |

> 说明：✅ 可操作，❌ 不可操作，— 无意义或不展示

### 2.4 用户特征
| 用户类型 | 特征描述 | 主要使用场景 |
|----------|----------|------------|
| | | |

### 2.5 系统约束
- 技术约束：
- 业务约束：
- 法规约束：

---

## 3. 业务目标 ⭐必填

### 3.1 问题描述
- **现状**：当前存在什么问题？（效率低/易出错/成本高/体验差）
- **影响范围**：影响哪些用户/部门/业务？
- **量化数据**：当前问题的量化指标是什么？

### 3.2 解决方案
- **核心思路**：如何解决这个问题？
- **关键功能**：需要哪些核心功能？
- **预期效果**：上线后期望达到什么效果？

### 3.3 成功标准
| 指标 | 当前值 | 目标值 | 衡量方式 |
|------|--------|--------|----------|
| | | | |

---

## 4. 角色权限 ⭐必填

### 4.1 角色定义
| 角色 | 说明 | 典型用户 |
|------|------|----------|
| | | |

### 4.2 权限矩阵
| 功能 | 角色1 | 角色2 | 角色3 |
|------|:-----:|:-----:|:-----:|
| 查看列表 | ✅ | ✅ | ❌ |
| 新增数据 | ✅ | ❌ | ❌ |
| 编辑数据 | ✅ | ❌ | ❌ |
| 删除数据 | ✅ | ❌ | ❌ |

### 4.3 数据权限
| 角色 | 数据范围 | 说明 |
|------|----------|------|
| | | |

---

## 5. 功能需求

### 5.1 需求概览

| 需求ID | 需求名称 | 优先级 | 来源 | 状态 |
|--------|----------|--------|------|------|
| FR-001 | | P0/P1/P2 | | 待开发 |

> 优先级：P0=必须实现，P1=重要，P2=一般

### 5.2 功能需求详述

#### FR-001：[需求名称]

| 项目           | 内容                               |
| ------------ | -------------------------------- |
| **需求ID**     | FR-001                           |
| **需求名称**     |                                  |
| **优先级**      | P0（必须实现）/P1（重要）/P2（一般）           |
| **业务目标**     | 解决什么问题？成功标准是什么？ |
| **需求描述**     | 作为 [用户角色]，我希望 [执行某操作]，以便 [达到某目标] |
| **目标用户**     |                                  |
| **触发条件**     | 用户操作（如点击菜单/按钮）或系统自动触发            |
| **输入/前置条件**  | 数据存在、权限满足、状态符合等                  |
| **执行步骤**     | 1. 2. 3.                         |
| **输出/后置动作**  | 数据变更/页面跳转/状态更新/通知发送等             |
| **界面原型**     |                                  |
| **异常处理**     | 1. [异常场景]：[现象]，处理：[系统响应]         |
| **其他规则**     | 1. 2.                            |
| **验收标准（AC）** | [ ] AC1： [ ] AC2：                |
| **依赖需求**     | 依赖哪些 FR                          |
| **备注**       |                                  |

##### 角色权限 ⭐必填

| 角色 | 可见性 | 可操作 | 数据范围 |
|------|--------|--------|----------|
| 角色1 | ✅ | 查看、编辑 | 全部 |
| 角色2 | ✅ | 查看 | 本部门 |
| 角色3 | ❌ | - | - |

##### 状态流转 ⭐必填

| 状态 | 可执行操作 | 操作角色 | 触发条件 | 下一状态 |
|------|-----------|----------|----------|----------|
| 状态A | 操作1 | 角色1 | 条件1 | 状态B |
| 状态A | 操作2 | 角色2 | 条件2 | 状态C |

##### 各角色在不同状态下的处理逻辑 ⭐必填

###### 角色1：申请人

| 状态 | 可执行操作 | 操作说明 | 前置条件 | 后置结果 |
|------|-----------|----------|----------|----------|
| 状态A | 操作1 | 说明 | 条件 | 结果 |
| 状态B | 操作2 | 说明 | 条件 | 结果 |

###### 角色2：审核人

| 状态 | 可执行操作 | 操作说明 | 前置条件 | 后置结果 |
|------|-----------|----------|----------|----------|
| 状态A | 操作3 | 说明 | 条件 | 结果 |

<!-- ====== 以下为 FR 模板中可选子章节 ====== -->
<!-- 注：根据需求具体内容选用，不相关的章节直接删除 -->

##### 查询条件字段说明
> 当需求涉及列表页查询/筛选功能时填写

| 条件项  | 输入方式                            | 默认值             | 规则                                      | 是否必填 |
| ---- | ------------------------------- | --------------- | --------------------------------------- | ---- |
| 条件名称 | 文本输入框/下拉选择框/日期选择框/日期范围选择器/数字输入框 | 默认显示的值，如"全部""空" | 校验规则、联动逻辑、预置文案 placeholder、字符长度限制、选项来源等 | 是/否  |

> **列说明**：
> - `条件项`：查询条件名称，与页面标签一致
> - `输入方式`：文本输入框/下拉选择框/日期选择框/日期范围选择器（两个date输入框用-连接）/数字输入框/多选列表框
> - `默认值`：页面加载时显示的默认值。下拉框通常为"全部"或"请选择"，日期框通常为空或当前月
> - `规则`：校验约束（格式/长度/范围）、联动逻辑（选A后过滤B）、预置文案 placeholder、选项数据来源等。规则内容较多时用分号或编号隔开
> - `是否必填`：查询条件通常不必填，但如有特殊要求（如必选月份）标注"是"

##### 操作按钮说明
> 当列表页顶部的工具栏/操作区有按钮时填写

| 按钮 | 位置 | 样式 | 交互规则 |
|------|------|------|----------|
| 按钮名称 | 工具栏左侧/右侧 | 主按钮（黑底白字）/默认按钮（白底灰边框）/危险按钮（红色） | 点击后的行为、前置校验、后置反馈等 |

> **列说明**：
> - `按钮`：按钮完整文案，含图标前缀如"+ 添加"
> - `位置`：工具栏左侧（主操作）/右侧（辅助操作）/行内
> - `样式`：btn-primary（主按钮黑底白字）/btn-default（默认按钮白底灰边框）/btn-danger（危险按钮红色）。主按钮每页不超过2个
> - `交互规则`：点击后行为、校验逻辑、loading状态、成功后反馈（Toast提示/页面跳转）、失败时反馈（错误提示），自定义多行的可添加行数上限

##### 行操作按钮说明
> 当列表中每行数据有操作按钮时填写

| 操作 | 显示条件 | 交互规则 |
|------|----------|----------|
| 查看 | 所有状态 | 点击跳转到详情页（新窗口打开或当前页跳转） |
| 编辑 | 仅草稿状态 | 点击跳转到编辑页，表单回显当前数据 |
| 删除 | 仅草稿状态 | 点击弹出确认对话框，确认后删除。有关联数据时阻止并提示 |

> **列说明**：
> - `操作`：操作按钮名称（查看/编辑/删除/生效/作废/归档等）
> - `显示条件`：哪些状态下显示此按钮。没有"显示条件"时简化为2列表
> - `交互规则`：点击后的完整行为描述，含确认弹窗文案、跳转路径、成功/失败反馈

##### 全选与翻页规则
> 当列表支持多选操作时填写

| 规则项 | 说明 |
|--------|------|
| 勾选全选 | 选中当页所有记录 |
| 取消单条 | 取消某条后全选勾选也自动取消 |
| 翻页 | 翻页后自动去掉已勾选记录及全选勾选 |
| 刷新页面 | 刷新后自动去掉所有勾选状态 |

##### 数据展示字段说明
> 当需求涉及列表/表格数据展示时填写

| 字段名称 | 数据来源 | 显示格式 | 说明 |
|----------|----------|----------|------|
| 字段名 | 数据表.列名/自动计算/自动编号 | 左对齐/右对齐 格式约束 | 特殊说明：如截断规则、hover浮层、颜色编码、颜色含义 |

> **列说明**：
> - `字段名称`：表格列头文案，与页面保持一致
> - `数据来源`：来自哪个数据表的哪个字段；或"自动编号""自动计算""手动录入"
> - `显示格式`：对齐方式、小数位数、千分位、日期格式(YYYY-MM-DD/YYYY-MM-DD HH:mm)、截断规则（最多N字符，超出显示…）。金额列统一"右对齐，保留2位小数，千分位分隔"
> - `说明`：特殊交互行为（hover浮层显示完整内容/点击跳转/颜色变化含义）、枚举值的含义映射

> **简化版（当无数据来源信息时）**：

| 字段名称 | 显示格式 | 说明 |
|----------|----------|------|
| 字段名 | 对齐 + 格式约束 | 特殊说明 |

##### 表单字段说明
> 当需求涉及表单输入/编辑时填写。按表单区块分别建表，如"基本信息表单字段""开票信息表单字段"

| 字段  | 输入方式                                          | 默认值         | 规则                                               | 是否必填 |
| --- | --------------------------------------------- | ----------- | ------------------------------------------------ | ---- |
| 字段名 | 单行文本输入框/下拉选择框/日期选择框/数字输入框/多行文本域/文件上传/复选框/单选按钮 | 默认值或"无"或"空" | 字符长度限制、校验规则（格式/范围/重复）、预置文案 placeholder、联动逻辑、只读条件 | 是/否  |


> **列说明**：
> - `字段`：字段名称，与页面标签文案一致
> - `输入方式`：单行文本输入框/多行文本域(textarea)/下拉选择框(select)/日期选择框(date)/数字输入框(number)/文件上传/复选框(checkbox)/单选按钮(radio)。可注明占列宽度如"占2列宽度""占4列宽度"
> - `默认值`：新增表单中显示的默认值。无默认值填"—"或"空"；下拉框默认选项通常为"请选择"
> - `规则`：包含以下信息（用分号或编号隔开）：
>   - 长度限制：最多N个字符
>   - 格式校验：正则或校验类型（手机号/邮箱/统一社会信用代码/纯数字/不允许特殊字符等）
>   - 值域范围：数字的min/max
>   - 预置文案 placeholder：如"请输入企业名称""请选择"
>   - 联动逻辑：选择A后B的选项被过滤
>   - 选项数据来源：系统字典/数据表/固定枚举
>   - 特殊行为：自动计算/回填/只读条件
> - `是否必填`：是/否。必填项以红色*号标识在标签前

##### 底部操作按钮
> 当表单页面底部有保存/取消等操作按钮时填写

| 按钮 | 样式 | 位置 | 交互规则 |
|------|------|------|----------|
| 保存 | 主按钮（黑底白字） | 左侧 | 提交前校验所有必填项和格式规则，校验不通过显示字段级错误提示。提交时间长时显示 loading。保存成功后 Toast 提示，返回列表页。 |
| 取消 | 默认按钮 | 保存按钮右侧 | 点击弹出确认提示"确认取消？未保存的内容将丢失"。确认后返回上级页面。 |

> 简化版（无需区分位置时）：

| 按钮 | 样式 | 交互规则 |
|------|------|----------|
| 保存 | 主按钮 | |
| 取消 | 默认按钮 | |

##### 详情页区域说明
> 当需求涉及详情/查看页面时填写

| 区域 | 展示内容 | 说明 |
|------|----------|------|
| 区域名称 | 包含的字段列表（用顿号分隔） | 数据来源、只读条件、特殊交互（如"点击查看全部跳转到列表页"） |

##### 页面布局说明
> 当页面由多个非标准区块组成时，用此表描述各区块

| 区域/区块 | 内容 | 说明 |
|-----------|------|------|
| 区块名称 | 包含的元素/字段 | 交互说明 |

##### 状态流转定义
> 当需求涉及业务状态流转时填写（仅涉及简单状态的模块用4列版本）


| 状态 | 定义 | 可操作 | 下一状态 |
|------|------|--------|----------|
| 状态A | 状态含义 | 允许的操作列表 | 可到达的下一状态 |

##### 交互规则（场景→规则形式）
> 当需求涉及多条件/多场景下的不同行为时填写

| 场景 | 规则 |
|------|------|
| 正常操作 | 描述 |
| 边界情况 | 描述 |
| 异常情况 | 描述 |

##### 约束规则
> 当需求有数据一致性/业务约束/校验规则时填写

| 规则 | 说明 |
|------|------|
| 规则名称 | 规则详细描述 |

##### 金额展示规则
> 当金额字段有特殊展示要求时填写

| 场景   | 规则                                                     |
| ---- | ------------------------------------------------------ |
| 正常金额 | 千分位分隔，保留2位小数，右对齐                                       |
| 负金额  | 红色文字（#ff4d4f）                                          |
| 金额为0 | 显示"0.00"                                               |
| 颜色编码 | 汇总金额加粗，应收金额橙色(#d46b08)，已收金额绿色(#52c41a)，未收金额红色(#ff4d4f) |

##### 步骤/向导结构
> 当需求为多步骤流程时有使用

| 步骤 | 操作区域 | 说明 |
|------|----------|------|
| Step 1: 步骤名称 | 此步骤包含的UI元素 | 交互说明 |

##### 导入导出字段映射
> 当需求涉及Excel导入/导出时填写

| Excel列名 | 必填  | 数据类型     | 说明        |
| ------- | --- | -------- | --------- |
| 列名（中文）  | 是/否 | 文本/数字/日期 | 格式说明、校验规则 |

##### 消息通知规则
> 当需求涉及消息/通知发送时填写

| 通知对象 | 触发条件 | 通知内容 | 通知方式 |
|----------|----------|----------|----------|
| | | | 站内信/邮件/短信/App推送 |

##### 数据维度定义
> 当需求涉及统计分析/报表时填写

| 数据维度 | 统计周期 | 数据定义 | 图表形式 | 数据来源 |
|----------|----------|----------|----------|----------|
| | | | 柱状图/折线图/饼图/表格 | |

##### 附件上传交互状态
> 当需求涉及文件上传时填写

| 状态 | 说明 |
|------|------|
| 上传前 | 虚线边框占位区域，点击触发文件选择对话框 |
| 上传中 | 显示进度条或loading图标 |
| 上传后 | 显示文件名标签+删除按钮 |
| 已录入 | 已保存的附件不可删除（仅查看/下载） |

---

## 6. 非功能需求

### 6.1 性能需求
| 指标 | 要求 | 测试场景 |
|------|------|----------|
| 页面响应时间 | P99 < 2秒 | 正常负载下 |
| 并发用户数 | 支持 N 并发 | 峰值压力测试 |
| 数据库查询响应 | < 500ms | 单次查询 |
| 批量操作 | N条 < X秒 | 批量生成/导出 |

### 6.2 安全需求
- 认证方式：
- 授权模型：（RBAC/ABAC）
- 数据加密：传输层 TLS 1.2+；敏感字段 AES-256
- 安全日志：操作审计日志保留 __ 天
- 合规要求：（如 GDPR/等保2.0）

### 6.3 可靠性需求
- 系统可用性：≥ 99.9%（SLA）
- RTO（恢复时间目标）：≤ __ 小时
- RPO（恢复点目标）：≤ __ 小时
- 数据备份：每日全量 + 实时增量

### 6.4 可维护性
- 代码覆盖率：≥ 80%
- 文档完整性：接口文档覆盖率 100%

### 6.5 兼容性
- 浏览器：Chrome 90+, Firefox 90+, Safari 14+, Edge 90+
- 移动端：iOS 13+, Android 9+

---

## 7. 系统接口需求

### 7.1 外部接口
| 接口名称 | 对接方 | 接口类型 | 优先级 | 备注 |
|----------|--------|----------|--------|------|
| | | REST/gRPC/消息队列 | | |

### 7.2 接口说明（每个接口）
- **接口名**：
- **地址**：
- **调用方式**：GET/POST/PUT/DELETE
- **请求参数**：（示例JSON）
- **响应格式**：（示例JSON）
- **认证方式**：
- **错误码**：

### 7.3 内部接口
| 接口名称 | 调用方式 | 说明 |
|----------|----------|------|
| | 同步/异步 | |

---

## 8. 数据需求

### 8.1 核心数据实体
| 实体 | 关键字段 | 说明 |
|------|----------|------|
| | | |

### 8.2 数据保留策略
| 数据类型 | 保留期限 | 处理方式 |
|----------|----------|----------|
| 操作日志 | 1年 | 到期自动删除 |
| 业务数据 | 永久 | 归档 |

---

## 9. 需求追踪矩阵（RTM）

| 需求ID | 需求名称 | 设计文档 | 代码模块 | 测试用例 | 状态 |
|--------|----------|----------|----------|----------|------|
| FR-001 | | | | TC-001 | ✅ |

---

## 10. 待确认问题

| 问题ID | 问题描述 | 提问人 | 提问日期 | 待确认方 | 状态 |
|--------|----------|--------|----------|----------|------|
| Q-001 | | | | | 待确认 |

---

## 附录 A：字段说明表格选用指南

| 场景        | 推荐表格     | 列数   | 备注                           |
| --------- | -------- | ---- | ---------------------------- |
| 列表页搜索条件   | 查询条件字段说明 | 5列   | 条件项 / 输入方式 / 默认值 / 规则 / 是否必填 |
| 列表页数据列    | 数据展示字段说明 | 4列   | 字段名称 / 数据来源 / 显示格式 / 说明      |
| 列表页顶部操作按钮 | 操作按钮说明   | 4列   | 按钮 / 位置 / 样式 / 交互规则          |
| 列表页行内操作按钮 | 行操作按钮说明  | 3列   | 操作 / 显示条件 / 交互规则             |
| 列表页多选逻辑   | 全选与翻页规则  | 2列   | 规则项 / 说明                     |
| 表单/录入页字段  | 表单字段说明   | 5列   | 字段 / 输入方式 / 默认值 / 规则 / 是否必填  |
| 表单页底部按钮   | 底部操作按钮   | 4列   | 按钮 / 样式 / 位置 / 交互规则          |
| 详情页区块     | 详情页区域说明  | 3列   | 区域 / 展示内容 / 说明               |
| 复杂布局页面    | 页面布局说明   | 2-3列 | 区域/区块 / 内容 / 说明              |
| 业务状态流转    | 状态流转定义   | 4-6列 | 完整版含CSS类名/颜色/允许操作/有效下一状态     |
| 多场景不同行为   | 交互规则     | 2列   | 场景 / 规则                      |
| 校验/约束     | 约束规则     | 2列   | 规则 / 说明                      |
| 金额特殊展示    | 金额展示规则   | 2列   | 场景 / 规则                      |
| 多步骤流程     | 步骤/向导结构  | 3列   | 步骤 / 操作区域 / 说明               |
| Excel导入导出 | 导入导出字段映射 | 4列   | Excel列名 / 必填 / 数据类型 / 说明     |
| 通知/消息发送   | 消息通知规则   | 4列   | 通知对象 / 触发条件 / 通知内容 / 通知方式    |
| 统计分析      | 数据维度定义   | 5列   | 维度 / 周期 / 定义 / 图表形式 / 数据来源   |
| 文件上传      | 附件上传交互状态 | 2列   | 状态 / 说明                      |
| 系统外部对接    | 外部接口     | 5列   | 接口名称 / 对接方 / 接口类型 / 优先级 / 备注 |

> **经验原则**：
> 1. 先看需求是什么场景（列表/表单/详情/流程/计算/接口），再从指南中选对应表格
> 2. 每种需求下的字段说明表，按页面上的区块（区域标题）分开建表，不要混在一起
> 3. 每张表格的"规则"列是最重要的——4-5列表格中唯一需要写长文本的列
> 4. 如果"规则"列内容超过3个约束点，建议拆分或用编号列表
> 5. 不要为每个页面都写所有表格——只写该 FR 实际需要的

---

*本文档依据 CMMI V3.0 REQM 过程域要求编制*
